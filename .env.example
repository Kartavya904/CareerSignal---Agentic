# ============================================================
# CareerSignal — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.
# ============================================================

# --- Database (PostgreSQL + pgvector) ---
# If your Postgres uses a different user/password, set DATABASE_URL in .env.local, e.g.:
#   DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@localhost:5432/careersignal
# Create the database first: createdb careersignal
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/careersignal
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=careersignal
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# --- Ollama (Local LLM Runtime) ---
# Start Ollama with a larger context to avoid "truncating input prompt" (e.g. set OLLAMA_CONTEXT_LENGTH=16384 before `ollama serve`).
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL_REASONING=deepseek-r1:32b-qwen-distill-q4_K_M
OLLAMA_MODEL_GENERAL=qwen2.5:32b-instruct-q4_K_M
OLLAMA_MODEL_CODE=qwen2.5-coder:32b-instruct-q4_K_M
OLLAMA_MODEL_FAST=llama3.1:8b-instruct-q4_K_M
OLLAMA_CONTEXT_LENGTH=16384
OLLAMA_EMBED_MODEL=mxbai-embed-large

# --- Playwright (Browser Automation) ---
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_SLOW_MO=0
BROWSER_RATE_LIMIT_MS=2000
SERPAPI_KEY=your_serpapi_key

# --- Application ---
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# --- Auth (required for sign up / sign in) ---
# Generate with: openssl rand -base64 24
AUTH_SECRET=your-secret-at-least-16-chars

# --- Artifact Storage ---
ARTIFACTS_DIR=./artifacts

# --- Deep Company Research (open-web search) ---
# Optional. When set, the Deep Company Dossier agent uses SerpAPI to discover real URLs
# (official site, Wikipedia, Reddit, careers, funding, etc.) and fetches those pages for enrichment.
# Get a key: https://serpapi.com/users/sign_up (free tier available).
# SERPAPI_KEY=your_serpapi_key
