/**
 * @careersignal/core — shared constants and utilities
 * (Connectors, fingerprint, dedupe moved to miscellaneous/backup with scope pivot.)
 */

export const APP_NAME = 'CareerSignal';
export const DEFAULT_TOP_K = 15;
